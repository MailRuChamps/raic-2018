namespace FSharpCgdk.Model

module Consts = 
    let ROBOT_MIN_RADIUS = 1.
    let ROBOT_MAX_RADIUS = 1.05 
    let ROBOT_MAX_JUMP_SPEED = 15.
    let ROBOT_ACCELERATION = 100.
    let ROBOT_NITRO_ACCELERATION = 30.
    let ROBOT_MAX_GROUND_SPEED = 30.
    let ROBOT_ARENA_E = 0.
    let ROBOT_RADIUS = 1.
    let ROBOT_MASS = 2.
    let TICKS_PER_SECOND = 60
    let MICROTICKS_PER_TICK = 100
    let RESET_TICKS = 2 * TICKS_PER_SECOND
    let BALL_ARENA_E = 0.7
    let BALL_RADIUS = 2.
    let BALL_MASS = 1.
    let MIN_HIT_E = 0.4
    let MAX_HIT_E = 0.5
    let MAX_ENTITY_SPEED = 100.
    let MAX_NITRO_AMOUNT = 100.
    let START_NITRO_AMOUNT = 50.
    let NITRO_POINT_VELOCITY_CHANGE = 0.6
    let NITRO_PACK_X = 20.
    let NITRO_PACK_Y = 1.
    let NITRO_PACK_Z = 30.
    let NITRO_PACK_RADIUS = 0.5
    let NITRO_PACK_AMOUNT = 100.
    let NITRO_PACK_RESPAWN_TICKS = 10 * TICKS_PER_SECOND
    let GRAVITY = 30.



